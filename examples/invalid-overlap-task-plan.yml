version: 1
runId: "invalid-overlap"
baseBranch: "main"

protected:
  - "src/generated/**"

agents:
  first:
    adapter: "generic"
    command: "echo first"
    task: "Touch too much code."
    owns:
      - "src/**"

  second:
    adapter: "generic"
    command: "echo second"
    task: "Touch generated code."
    owns:
      - "src/generated/**"
